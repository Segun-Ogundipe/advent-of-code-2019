import { firstWire, secondWire } from './data/puzzle-input';
/**
 * Returns an integer as the result of a gravity assist algorithm.
 *
 * @param intCode - An intcode array
 */
function manhattanDistance(firstWire: String[], secondWire: String[]): number {
  return 0;
}

console.log(manhattanDistance(firstWire, secondWire));

export { manhattanDistance };
